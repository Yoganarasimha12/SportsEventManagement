console.log("emailModalData being sent to modal:", {
  creditStatus: props.applicationInfo?.applicationStatus,
  customerName: fullName,
  card_type: props.applicationInfo?.card_type,
  deliveryStatus: cardInfo?.deliveryStatus,
});